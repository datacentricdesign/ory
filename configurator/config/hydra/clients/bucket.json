{
  "client_id": "${BUCKET_CLIENT_ID}",
  "client_name": "Bucket",
  "description": "A Data cloud for Data-Centric Design Lab.",
  "client_secret": "${BUCKET_CLIENT_SECRET}",
  "scope": "",
  "grant_types": ["client_credentials", "refresh_token"],
  "response_types": ["token", "refresh_token"]
}
