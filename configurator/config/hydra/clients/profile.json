{
  "client_id": "${PROFILE_CLIENT_ID}",
  "client_name": "Profile",
  "description": "An Identity Provider of the Data-Centric Design Lab.",
  "client_secret": "${PROFILE_CLIENT_SECRET}",
  "scope": "${PROFILE_SCOPE}",
  "grant_types": ["client_credentials", "refresh_token"],
  "response_types": ["token"]
}
